<%= form_for(garage) do |f| %>
  <% if garage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(garage.errors.count, "error") %> prohibited this garage from being saved:</h2>

      <ul>
      <% garage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :location %>
    <%= f.text_field :location %>
  </div>

  <div class="field">
    <%= f.label :daily_price_cents %>
    <%= f.number_field :daily_price_cents %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
